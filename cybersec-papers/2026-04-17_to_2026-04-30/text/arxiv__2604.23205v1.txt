Tessera: Secure, Near-Line-Rate Weight
Streaming for UMA Edge Accelerators
Animan Naskar
Indian Institute of Technology (IIT) Ropar
2022csb1297@iitrpr.ac.in
Abstract. Deploying proprietary Deep Neural Networks (DNNs) on
commodity edge devices demands hardware-backed Digital Rights Man-
agement (DRM) capable of withstanding both software-level and physical
adversaries. In Unified Memory Architecture (UMA) systems, the host
CPU and Neural Processing Unit (NPU) share physical DRAM, leaving
plaintext model weights directly readable by a compromised OS kernel.
Existing defenses fail in this constrained setting: trusted execution envi-
ronments monopolize scarce memory with permanently reserved regions,
while full-memory encryption operates at page granularity. This forces
the system to fetch massive 4 KB memory pages for sub-page tensor tiles,
severely crippling bandwidth.
We present Tessera, a reference architecture for inline, cache-line-
granularity weight decryption on UMA edge accelerators. The design
intercepts 64-byte AXI bursts, computing AES-256-CTR keystreams in
parallel with DRAM fetches. This streams plaintext directly into iso-
lated NPU SRAM, creating a transient memory footprint confined to the
active tile and eliminating the need for permanent memory carve-outs.
Measurements across three distinct SoC platforms demonstrate that this
parallelization hides cryptographic latency behind standard DRAM fetch
times, a condition that holds even under worst-case timing variations.
Consequently, Tessera is projected to achieve 98.4% of the theoretical
memory bandwidth ceiling (a mere 1.6% overhead). Across standard
vision and language models, page-level memory encryption suffers up to
a 32× bandwidth penalty, whereas Tessera maintains an optimal 1×
footprint for all layer geometries. Finally, Tessera neutralizes major
UMA-specific attack vectors—including physical DRAM extraction, rogue
DMA, and compute hijacking—and formally prevents plaintext leakage
across sparse tensors.
Keywords: Edge AI · Hardware Security · Unified Memory Architecture ·
Inline Crypto Engine · AES-CTR · DRM · Neural Processing Unit · Cache-Line
Encryption
1
Introduction
Modern edge inference platforms ship with NPUs capable of tens of TOPS,
enabling DNN deployment in applications ranging from autonomous vehicles
arXiv:2604.23205v1 [cs.CR] 25 Apr 2026

to medical diagnostics. The commercial value of underlying model weights is
substantial: manufacturers increasingly treat the model binary as a licensable
asset subject to per-device or per-inference DRM.
Weight protection is fundamentally harder on edge devices than in cloud set-
tings for three primary reasons. First, edge devices are physically accessible; cold-
boot attacks and DRAM interposer probing are well-documented on consumer
hardware [3,4]. Second, the OS attack surface is far larger on resource-constrained
IoT devices [5]. Third, the UMA topology prevalent in mobile SoCs—exemplified
by ARM Mali and NVIDIA Jetson—places the CPU DRAM bus on a path
that physically intersects the accelerator’s tensor-tile fetch stream, leaving any
plaintext in shared DRAM trivially accessible to a kernel-level attacker.
These constraints undermine mainstream defenses. Full-DRAM encryption
(AMD SME [6], Intel TME [7]) operates at page granularity, causing massive traffic
amplification for sub-page tensor tiles. CPU-hosted TEEs (SGX [8], TrustZone [9])
introduce data-copy overheads that are difficult to hide in real-time edge pipelines.
Furthermore, systems that rely on statically reserved protected-memory regions
(PVM-style enclaves) permanently reduce the usable system RAM available to
the OS and co-located workloads.
This paper. Tessera is a vendor-agnostic reference architecture that ad-
dresses these UMA vulnerabilities by confining decryption to a dedicated Inline
Crypto Engine (ICE) physically interposed on the AXI bus. It decrypts weights
just-in-time directly into the NPU’s on-chip SRAM, ensuring plaintext exists
only transiently without carving out protected regions of main memory. Our
contributions are:
– Cache-line-granularity ICE for UMA NPUs (section 3): A concrete
reference architecture for 64-byte AXI-interposed decryption on UMA SoCs,
featuring a consistent RSA-based key hierarchy, address-derived AES-CTR
counters, and SMMU stream-ID isolation.
– Empirical demonstration of full weight extraction (section 5.2):
A cross-model demonstration (ResNet-18, MobileNetV2, DistilBERT-Tiny)
proving that 100% of DNN weights are trivially recoverable on a Jetson AGX
Xavier via /dev/mem with zero exploit, directly validating the UMA threat
model.
– Formal necessity of address-derived counters (section 3.2): A proof
that any fixed-counter scheme leaks plaintext via XOR cancellation over sparse
DNN tensors, establishing address-derived derivation as a strict cryptographic
requirement for cache-line DRM.
– Near-line-rate throughput and optimized energy efficiency (sec-
tion 5): Hardware measurements confirm Tessera safely hides cryptographic
overhead behind standard DRAM fetch times, projecting a throughput of
22.1 GB/s (98.4% of the DDR5-4800 peak ceiling). Unlike page-level defenses
that force massive 4 KB fetches—imposing up to a 32× bandwidth penalty—
Tessera secures data at the hardware’s native 64-byte granularity. This
eliminates structural traffic amplification and saves over 22 mJ of DRAM
PHY energy per inference.

2
Background and Motivation
2.1
Unified Memory in Edge Accelerators
Unlike discrete GPU configurations with separate PCIe-attached DRAM, UMA
SoCs instantiate a single physical DRAM array shared by all on-chip masters:
CPU clusters, NPU, image signal processor, and DMA engines. The NPU reads
tensor tiles via large sequential DMA bursts, bypassing the CPU L3 cache
hierarchy entirely. A consequence is that CPU-side MMU protections do not
prevent a DMA engine from issuing reads to the same physical pages; isolation
additionally requires SMMU stream-ID firewall rules.
2.2
Limitations of Page-Level Encryption
For a tensor tile of t bytes where t ≪4096, decrypting a full page amplifies
effective DRAM traffic by ⌈4096/t⌉. This reaches 32× for batch-normalisation
parameter fetches (t = 128 B) and 15× for depth-wise convolution weight tiles
(t = 288 B). Moreover, page-level ICEs expose decrypted plaintext in the CPU-
accessible memory fabric—a concession a kernel-privileged attacker can exploit
via straightforward /dev/mem reads on UMA devices.
2.3
Limitations of Reserved Secure Memory
A common alternative to shared-DRAM protection is to execute sensitive work-
loads inside a statically reserved protected-memory region, such as a TEE-backed
carve-out or PVM-style secure buffer. This approach improves confidentiality,
but it creates a direct capacity trade-off: the reserved region is no longer available
to the host OS, so usable system memory is reduced by exactly the amount
carved out for security. On memory-constrained edge devices, that trade-off is
often expensive; for example, reserving 2 GB of protected memory on an 8 GB
phone leaves only 6 GB for normal applications and services. In addition, the
secure region must be sized for worst-case model footprints even though inference
touches only a small tile at a time, so much of the reserved memory sits idle
during execution.
Tessera avoids this penalty by keeping plaintext out of shared DRAM
entirely and materializing it only just in time inside the NPU’s on-chip SRAM.
The result is a small transient footprint rather than a permanently reserved
secure-memory carve-out.
2.4
Threat Model
Definition 1 (Adversary Capabilities). We assume a powerful adversary
with full control over the host OS and physical access to off-chip components.
Specifically, the adversary possesses: (1) System Compromise: Root/kernel
execution on the host CPU, including full IOMMU page-table write access; (2)
Rogue DMA: The ability to issue arbitrary DMA transactions from malicious

peripherals to any SMMU-unprotected address; (3) Physical DRAM Access:
PCB access enabling cold-boot extraction [3] or DRAM bus interposer probing; and
(4) Traffic Manipulation: The capability to replay, reorder, or alias ciphertext
blocks presented to the ICE.
Definition 2 (Trust Boundary). The physical SoC die boundary is trusted.
Consistent with standard hardware security models (e.g., ARM TrustZone [9],
AMD PSP [10]), we explicitly exclude invasive silicon attacks (e.g., decapping),
fault injection (e.g., glitching), and OEM supply-chain tampering. Physical side-
channel analysis of the ICE is likewise out of scope; architectural mitigations for
these are discussed in section 8.
3
The Tessera Reference Architecture
Figure 1 summarises the Tessera datapath and trust boundary.
Fig. 1. Tessera hardware pipeline. The ICE sits on the path between the DRAM
controller and the NPU DMA fabric. The secure enclave provisions the AES session
key directly into ICE registers; address-derived counters enable parallel keystream
generation; and the SMMU firewall blocks host access to plaintext SRAM.
3.1
Hardware Root of Trust and Key Hierarchy
Tessera relies on a three-layer key hierarchy anchored by a hardware root of
trust, utilizing RSA with OAEP padding (RSAES-OAEP [1]) for secure key
delivery.

Layer 1 — Device Identity Keypair (DIK). During device provisioning,
the OEM generates (or injects) a unique 2048-bit (or 4096-bit) RSA keypair
(pk dev, sk dev). The public key pk dev is embedded in a device certificate, while the
private key sk dev is fused into on-die eFuses and accessible only to a hardware
Secure Enclave via a restricted interface. The key has no software-visible interface
and is never exposed to shared DRAM.
Layer 2 — Per-Model Session Key (MSK). For each model, the developer
generates a random 256-bit AES key (kmsk) and encrypts weights using AES-
256-CTR. The key is provisioned via a bound blob:
B = RSAES-OAEP(pk dev, kmsk ∥Happ)
(1)
where Happ = SHA-256(code-signing certificate). Encryption under pk dev ensures
device binding, while including Happ enables application binding, enforced by the
Secure Enclave at load time. Any modification causes OAEP decoding to fail
with overwhelming probability.
Layer 3 — ICE Register Provisioning. At load time, the untrusted OS
supplies B to the Secure Enclave, which decrypts it using sk dev to recover
kmsk ∥Happ. The Enclave verifies the caller’s code-signing identity (via secure
boot/attestation) against Happ. If valid, it provisions kmsk into ICE registers via
a secure on-die bus inaccessible to CPU and DMA; otherwise, the load aborts.
The session key is never written to shared DRAM.
3.2
Cryptographic Primitive: Cache-Line AES-CTR
Out-of-order NPU DMA fetches preclude sequential modes (e.g., CBC, CFB).
Tessera therefore uses AES-CTR with address-derived counters. Let L = 64
bytes, IV base be a 96-bit per-model nonce stored in B, and Paddr be a cache-line-
aligned physical address. The counter is defined as:
CTR(Paddr) = IV base ∥
Paddr
L

(2)
forming a 128-bit AES input from the nonce and a 32-bit block index. To prevent
keystream reuse across models or executions with overlapping address spaces,
IV base provides per-model domain separation.
Proposition 1 (Counter Uniqueness). If
j
P (1)
addr/L
k
̸=
j
P (2)
addr/L
k
then
CTR(P (1)
addr) ̸= CTR(P (2)
addr), for weight blobs up to 232 × L = 256 GiB (i.e.,
without counter wraparound).
Proposition 2 (Address-Aliasing Resistance). Remapping physical pages
to present the same ciphertext at address P ′ ̸= P causes the ICE to apply
CTR(P ′) ̸= CTR(P), yielding incorrect plaintext with overwhelming probability.
This neutralises adversary capability (4) from section 2.4.

3.3
The ICE Execution Pipeline
Algorithm 1 formalises the per-cache-line ICE operation.
Stage 1: DMA Intercept. The ICE captures each cache-line-granularity read
request on the AXI bus and extracts the target physical address Paddr.
Stage 2: Parallel Keystream and Fetch. The ICE computes CTR(P) per
eq. (2) in one cycle, then initiates AES-256 forward encryption. A pipelined
AES-256 core with R pipeline stages produces the keystream in Tks = R cycles.
Concurrently, the DRAM fetch traverses the memory controller and DDR PHY.
Because TDRAM ≫Tks on all evaluated platforms (section 5.3), keystream
completion occurs before ciphertext arrival in the common case.
Stage 3: Line-Rate XOR. The ICE XORs the 64-byte burst combinatorially.
The XOR stage is pipelined into AXI datapath, adding only 1-2 cycles of delay.
Stage 4: Isolated SRAM Write. Plaintext is placed on the AXI write channel
to the NPU’s L2 SRAM, protected by the SMMU stream-ID firewall (section 3.4).
Algorithm 1 ICE Per-Cache-Line Decrypt (Tessera)
Require: Physical address Paddr, ciphertext C[0..L−1], key kmsk, base nonce IV base
Ensure: Plaintext M[0..L−1] written to isolated SRAM
1: ctr ←IV base∥⌊P/L⌋
▷1 cycle; address arithmetic
2: KS ←AES-256(kmsk, ctr)
▷parallel with DRAM fetch
3: await DRAM burst C on AXI bus
4: M ←C ⊕KS
▷bitwise XOR, 1–2 cycles
5: write M to isolated NPU SRAM
3.4
Enforcing Unified Memory Isolation
SMMU Stream-ID Firewalling. The SoC System Memory Management
Unit (SMMU) is configured in TrustZone Secure World to assign the NPU DMA
engine a dedicated stream ID that maps exclusively to the protected SRAM range.
Any CPU-initiated access to this range results in a bus abort (SLVERR/DECERR
on AXI). The SMMU configuration registers are TrustZone-locked and are not
writable by Normal World software.
SMMU Trust Assumptions. Tessera assumes correct SMMU configuration
by trusted firmware and the absence of implementation flaws. Prior work has
demonstrated that misconfiguration or firmware vulnerabilities (e.g., [13]) can
undermine IOMMU-based isolation. Thus, SMMU enforcement is not inherently
fail-safe and must be treated as part of the trusted computing base.
Hardware Tag Propagation. For defense in depth, Tessera leverages hard-
ware tagging where available (e.g., ARM MTE or equivalent coloring mechanisms).
The ICE annotates plaintext transactions with a restricted tag that is enforced
by the interconnect on subsequent accesses, preventing unauthorized agents from
dereferencing protected data even in the presence of SMMU misconfiguration.

4
Preemption and Context-Switch Safety
In multi-process environments, OS preemption can interrupt the NPU mid-
inference. Without safeguards, decrypted weights may persist in SRAM and be
exposed to subsequent contexts. Tessera prevents such leakage via the hardware
preemption hook in algorithm 2.
Algorithm 2 Tessera Preemption Hook (Revised)
Require: Preemption signal σ
Ensure: No plaintext persists across the context boundary
1: Stop issuing new DMA requests from NPU
2: Drain in-flight AXI transactions
3: Zero-fill / invalidate plaintext SRAM via hardware scrub engine
4: Clear ICE key registers
5: OS proceeds with context switch
On resume:
6: Re-provision kmsk into ICE from Secure Enclave
7: Restart execution from last tile boundary (re-fetch from DRAM)
Hardware-Enforced Execution. To tolerate a compromised OS, SRAM zero-
fill (Step 3) is performed by a dedicated hardware scrub engine within the ICE
datapath, triggered via a privileged control path inaccessible to the CPU. This
ensures that plaintext removal cannot be bypassed or delayed by software.
Preemption Latency. The dominant cost is SRAM zero-fill:
Tpreempt =
SSRAM
BW SRAM
+ Tsave
(3)
where Tsave ≈1.5 µs includes state save and TrustZone switching.
As shown in table 1, Tpreempt is well below typical OS context-switch latency
(≥100 µs). For example, a 2 MB SRAM at 512 GB/s yields Tpreempt ≈5.4 µs,
indicating negligible scheduling impact.
Table 1. Instantiated Tpreempt for evaluated NPU configurations. Formula: Tpreempt =
SSRAM /BW SRAM + Tsave (Tsave = 1.5 µs).
Platform (NPU)
SSRAM
BW SRAM
Tpreempt
Intel i9-12900H (iGPU L2$)
2 MB
512 GB/s
5.4 µs
Jetson AGX Xavier (DLA)
4 MB
480 GB/s
9.8 µs
Jetson AGX Orin (DLA)
4 MB
960 GB/s
5.7 µs
OS scheduler context-switch floor
≥100 µs

5
Evaluation
5.1
Methodology and Scope
As Tessera is a reference architecture pending silicon fabrication, we evaluate it
using a combination of empirical measurements on proxy hardware and analytical
modeling. Our methodology is designed to establish feasibility and bounding char-
acteristics along four axes: (i) UMA vulnerability validation, (ii) cryptographic
vs. memory latencies, (iii) bandwidth amplification, and (iv) pipelined execution
throughput.
Table 2 summarizes the experimental configurations. While our analytical
models capture fundamental architectural latencies and structural bottlenecks,
they abstract away microscopic DRAM scheduling, refresh cycles, and interconnect
contention. Consequently, the modeled throughputs represent idealized upper
bounds.
Table 2. Evaluation methodology summary.
Component
Type
Platform
/
Workload
Description
UMA Threat Validation
Measured Jetson
AGX
Xavier
Byte-accurate,
exploit-free
model weight recovery via
memory mapping.
AES & DRAM Latencies Measured i9, Xavier, Orin OpenSSL AES-NI and mem-
ory probes used as hardware
timing proxies.
Bandwidth Amplification Modeled
ResNet,
Mo-
bileNet, BERT
Analyzes
page-level
traffic
penalty vs. Tessera’s 64 B
cache-line granularity.
End-to-End Throughput
Modeled
i9, Xavier, Orin Compares Direct AES (block-
ing) vs. Tessera (pipelined)
using measured latencies.
Preemption Latency
Modeled
3 platforms
Estimates
for
hardware
context-switching and SRAM
zero-fill.
5.2
UMA Threat Validation
To empirically validate the UMA threat model (section 2.4), we executed an
end-to-end extraction attack on a Jetson AGX Xavier. The attack requires only
standard root privileges and utilizes no kernel exploits.
1. Address Resolution: We loaded ResNet-18, MobileNetV2, and DistilBERT-
Tiny via PyTorch. Using the standard nvmap IOCTL interface (/dev/nvmap),
we resolved the GPU-virtual addresses of the weight tensors into physical
DRAM addresses.

2. Direct Extraction: The attacker process mapped these physical addresses
directly into its own virtual space via /dev/mem using mmap(), reading the
tensor bytes directly from the shared DRAM bus.
Results and Mitigation. We achieved 100% byte-for-byte recovery of the
PyTorch state_dict() tensors. This confirms that physical DRAM sharing
in edge UMA architectures constitutes a trivial, low-barrier attack vector for
privileged adversaries.
Tessera completely neutralizes this extraction path. Because the NPU fetches
weights through the ICE, the data residing in shared DRAM is strictly AES-CTR
ciphertext, rendering /dev/mem reads unintelligible. The decrypted plaintext is
materialized only transiently inside the NPU’s SRAM, which is strictly isolated
from CPU-originated reads by the SMMU stream-ID firewall (section 3.4).
5.3
AES-CTR Latency Feasibility
Tessera’s near-line-rate claim rests on the empirical condition Tks < TDRAM .
We measure both quantities on three representative platforms.
Tks measurement. AES-256-CTR keystream latency for a 64-byte pay-
load is measured using OpenSSL 3.0 with AES-NI over 107 iterations via
CLOCK_MONOTONIC_RAW. We report the median latency after 1000 warm-up it-
erations. These measurements provide an upper bound on software keystream
latency; dedicated hardware AES pipelines are expected to achieve comparable
or lower latency under steady-state operation.
TDRAM measurement. We measure DRAM access latency using a pointer-
chasing benchmark over a randomly permuted linked list whose working set
exceeds the LLC capacity. The data-dependent traversal prevents prefetching
and enforces serialized cache misses, so each dereference incurs a full DRAM
access. The average per-dereference latency is reported as TDRAM .
Table 3 shows that ∆= TDRAM −Tks is positive across all evaluated platforms,
ranging from 26.4 ns to 67.4 ns. Normalised slack ∆/TDRAM lies between 61%
and 94%, indicating substantial headroom for overlap.
The latency model reduces to:
Tline = Taddr + max

Tks,
L
BWmem

+ TXOR
(4)
Under this condition, keystream generation is fully hidden beneath DRAM
latency, leaving the XOR stage (1-2 pipeline cycles in general) as the dominant
additional cost.

Table 3. Measured Tks (AES-NI software, 107 iterations, median) and TDRAM (pointer-
chasing, DRAM-resident list) for a 64-byte cache line. ∆= TDRAM −Tks; zero-overhead
pipelining requires ∆> 0.
Platform
Memory
Tks
TDRAM
∆
Intel i9-12900H
DDR5-4800
4.2 ns
71.6 ns
67.4 ns
Jetson AGX Xavier
LPDDR4x
16.8 ns
43.2 ns
26.4 ns
Jetson AGX Orin
LPDDR5X
12.1 ns
38.7 ns
26.6 ns
Zero-overhead condition: ∆> 0
✓
5.4
Jitter-Aware Pipeline Robustness
Table 3 shows that ∆= TDRAM −Tks > 0 under nominal conditions. While this
guarantees overlap for a single request, sustained throughput depends on whether
positive slack is maintained under latency variation across a continuous DMA
stream.
Simulation model. We simulate a sequence of 105 synthetic 64-byte cache-
line requests, modeling a steady NPU DMA stream. For each request i, AES
keystream generation and DRAM access latencies are modeled as independent
random variables:
T (i)
ks ∼N(Tks, σ2
ks),
T (i)
DRAM ∼N(TDRAM , σ2
DRAM ),
with σks ≈0.1 Tks and σDRAM ≈0.2 TDRAM to capture first-order timing
jitter (e.g., row-buffer effects and contention). The pipeline enqueues keystream
blocks and consumes them upon DRAM completion; a stall is recorded if the
corresponding keystream is not ready when ciphertext arrives.
Results. Across all evaluated platforms, stall probability remains below 0.1%, in-
dicating that positive slack is robust to moderate latency variation. The maximum
observed keystream buffer occupancy is ≈3.7 KB (58 cache lines), providing a
practical upper bound for SRAM sizing. These results confirm that the condition
TDRAM > Tks is not only sufficient in the nominal case but also stable under
realistic jitter.
Limitations. The model abstracts away detailed DRAM scheduling effects (e.g.,
bank conflicts, refresh timing) and memory-level parallelism. As such, it should
be interpreted as a first-order robustness check rather than a cycle-accurate
performance model. Bandwidth scaling is analysed separately in section 5.6.
5.5
Bandwidth Amplification on Real DNN Workloads
To quantify the inefficiency of page-level decryption, we evaluate the bandwidth
amplification factor—the ratio of physically fetched bytes to utilized bytes.
Tessera largely eliminates this overhead by intercepting DMA requests at the
minimum architectural fetch unit (the L = 64 B cache line) without speculative
prefetching. For a tensor tile of |T| bytes, Tessera generates exactly ⌈|T|/L⌉×L

bytes of DRAM traffic. Aligned tiles incur zero overhead. For unaligned tiles, the
maximum penalty is just 63 B per tile, resulting in a worst-case amplification
≤1.016× for any tile ≥4 KB. Conversely, a page-level ICE must fetch a full
4 KB page for any access t, incurring a rigid ⌈4096/t⌉multiplier regardless of
alignment.
Impact on Standard Architectures. Figure 2 plots the page-level amplifica-
tion penalty across representative layer types from ResNet-18, MobileNetV2, and
DistilBERT-Tiny. The practical impact is stark: lightweight edge models heavily
utilize small tiles. For example, batch normalization (128 B) and depth-wise con-
volutions (288 B) force massive page-level penalties of 32× and 15×, respectively.
Tessera avoids this entirely, maintaining an optimal 1× baseline across all layer
geometries.
0
10
20
30
FC layer
Attn QKV proj
Conv 3×3 (large)
PW Conv (wide)
Conv 3×3 (mid)
PW Conv (narrow)
DW Conv 3×3
Batch Norm
Tessera baseline
1
1
1
2
4
8
15
32
Bandwidth amplification A(t)
Fig. 2. Page-level ICE bandwidth amplification A(t) = ⌈4096/t⌉by layer type (tile sizes
t from TensorRT/Gemmini schedules [18,19]): Batch Norm 128 B; DW Conv 3×3 288 B;
PW Conv narrow 512 B; Conv 3×3 mid 1024 B; PW Conv wide 2048 B; Conv/Attn/FC
≥4096 B (A = 1). Tessera holds A ≈1× throughout (dashed line).
5.6
End-to-End Weight-Streaming Throughput
Having established the bandwidth penalty of page-level granularity, we isolate
the performance impact of our second structural contribution: the parallel, out-
of-order execution pipeline. Effective throughput BWeff depends on the hardware
memory ceiling BWceiling, the amplification factor A(t) (1.0× for Tessera), and
the exposed cryptographic latency Tcrypto relative to the memory fetch latency
TDRAM:
BWeff = BWceiling
A(t)
×

TDRAM
TDRAM + Tcrypto

(5)
Table 4 demonstrates the necessity of Tessera’s pipelined architecture across
all three evaluated platforms, utilizing hardware latency measurements from
Section 5.3.

A naive architecture applying AES directly to the fetched data (e.g., direct
block decryption) fundamentally cannot begin cryptography until the ciphertext
arrives from memory. This data dependency exposes the full AES computation
time as a blocking bus stall (Tcrypto = TAES). Analytically, this inherently
sequential behavior throttles throughput significantly, reducing bandwidth on
the Jetson AGX Xavier to just 72.0% of the hardware ceiling.
In contrast, Tessera leverages address-derived AES-CTR, which completely
decouples the cryptographic primitive from the data arrival. The ICE computes
the keystream entirely in the shadow of the DRAM fetch (TDRAM > TAES).
Because the keystream finishes early, the only exposed latency is a 1–2 cycle
combinational XOR delay to merge the streams (Tcrypto = TXOR). Amortized
over a standard 128-transfer AXI burst, this negligible penalty allows Tessera
to sustain ∼98.5% of maximum unencrypted throughput across all edge devices.
Table 4. Throughput normalized to the unencrypted hardware ceiling. The direct
decryption model utilizes measured AES latencies from Table 3 as a blocking delay.
Platform
Lat. (ns)
Throughput (% of Max)
TAES
TDRAM
Base Direct†
Tessera‡
i9-12900H (DDR5)
4.2
71.6
100
94.4
98.5
Xavier (LPDDR4x)
16.8
43.2
100
72.0
98.5
Orin (LPDDR5X)
12.1
38.7
100
76.2
98.5
†Direct decryption is sequential (TDRAM/(TDRAM + TAES )).
‡CTR keystream is pre-computed. Sustains only a 2-cycle TXOR penalty per 128-cycle AXI burst
(128/130 ≈98.5%).
5.7
Theoretical Silicon Area and Power Overheads
Complementing the performance analysis, we project the physical PPA overheads
of the Tessera ICE from established ASIC synthesis benchmarks.
Silicon Area. To saturate the measured memory ceiling of 22.4 GB/s (179.2 Gbps),
the ICE must evaluate 1.4×109 AES blocks per second (16 bytes per block). This
throughput is comfortably achieved by instantiating either two parallel 128-bit
AES-256 datapaths at 700 MHz or a single fully-pipelined core at 1.4 GHz. A
high-throughput AES-256 pipeline requires approximately 100,000 Gate Equiva-
lents (GE). In a standard 28 nm CMOS node (1 GE ≈0.4 µm2), the footprint
evaluates to:
100,000 GE × 0.4 µm2/GE = 40,000 µm2 ≈0.04 mm2
Compared to modern edge AI dies (e.g., the Jetson AGX Orin at ≈300 mm2),
the ICE consumes less than 0.02% of the total silicon area.
Power Consumption. High-throughput hardware AES-256 in 28 nm achieves
an energy efficiency of approximately 0.5 pJ/bit. Operating continuously at the

peak sustained bandwidth of 179.2 Gbps yields:
179.2 Gbps × 0.5 pJ/bit ≈90 mW
This 90 mW draw is functionally negligible against the multi-watt power envelopes
of the DDR5 PHY and NPU compute units, confirming that the Tessera ICE
is effectively invisible against the host SoC’s PPA budget.
5.8
System-Level Energy and Storage Projections
Beyond raw throughput, edge accelerators are strictly constrained by battery life
and on-chip SRAM capacity.
DRAM Access Energy Net-Savings. Off-chip LPDDR5/DDR5 accesses
consume approximately 15 pJ/bit (120 pJ/byte). We compare the energy required
to load the 46.8 MB ResNet-18 model under both paradigms:
– Page-level ICE (5.0× amplification): Requires ≈28.0 mJ of DRAM PHY
energy per inference.
– Tessera ICE (1.0× amplification): Consumes only ≈5.6 mJ in DRAM
access, plus an additional 0.19 mJ to operate the 90 mW ICE for the 2.12 ms
load time.
Therefore, Tessera yields a net energy saving of > 22 mJ per inference. This
demonstrates that cache-line granularity is not merely a performance optimisation,
but a fundamental energy necessity for edge DRM.
SRAM Keystream Buffer Sizing. Because Tessera pipelines keystream
generation ahead of the DRAM payload, it requires an on-chip FIFO to hold
keystreams until the corresponding ciphertext arrives. By Little’s Law (Data =
Bandwidth×Latency), sustaining 22.4 GB/s against a worst-case DRAM row-miss
latency of ≈100 ns requires a steady-state buffer of:
22.4 GB/s × 100 ns = 2,240 bytes
To safely absorb the transient latency spikes identified in our jitter simulation
(section 5.4), we provision a 4 KB SRAM FIFO. This fully decouples keystream
generation from DRAM latency variation while representing a negligible area
overhead.
Memory Capacity Impact. Unlike PVM-style secure-memory designs that
reserve a fixed region of main memory for protected execution, Tessera adds
no persistent DRAM carve-out. The only additional storage is the small on-chip
FIFO above and the existing NPU SRAM used transiently during inference. This
means the host operating system retains full access to main memory outside the
brief tile window in which plaintext is live.
6
Security Analysis
We analyse the four principal attack classes against UMA model confidentiality
and the corresponding Tessera countermeasure.

Attack 1: Physical DRAM Extraction (Cold-Boot & Interposer). An ad-
versary freezes DRAM after power-off or probes the memory bus. Countermeasure:
All weight data in shared DRAM is exclusively AES-256-CTR ciphertext under
kmsk. The key is provisioned directly from the Secure Enclave into ICE registers
and never written to DRAM; bus or DRAM capture yields only ciphertext.
Attack 2: Rogue DMA and Unsafe-Place Decryption. A compromised
host OS programs a rogue peripheral or misuses DMA to read plaintext from
NPU SRAM and exfiltrate it to general memory. Countermeasure: The SMMU
Stream-ID firewall (section 3.4) restricts the protected SRAM range to the NPU
DMA stream ID. Any other initiator triggers a hardware bus abort, and the
policy is enforced from TrustZone Secure World.
Attack 3: Preemption-Based Compute Hijack. The attacker preempts a
legitimate inference after weights have been decrypted into NPU SRAM and
schedules a malicious follow-on task to leak the residual plaintext through normal
outputs. Countermeasure: The hardware preemption hook (section 4, algorithm 2)
intercepts the context switch before it completes and atomically zero-fills the
plaintext SRAM via a secure sideband path. The attacker inherits only cleared
state.
Attack 4: Confused Deputy and Malicious Sibling. The attacker modifies
the approved application, reuses a stolen blob in a different application, or
tampers with the blob in transit. Countermeasure: The key blob B (eq. (1)) binds
kmsk to Happ. At model load time, the Secure Enclave verifies that the calling
application’s live signature matches Happ extracted from B. Modified code fails
the hash check; impostor applications present the wrong identity; and tampered
blobs fail OAEP verification.
7
Related Work
Full-memory encryption. AMD SME [6] and Intel TME [7] encrypt DRAM
using a single ephemeral key, protecting against cold-boot attacks, but operate
at 4 KB page granularity designed for CPU cache hierarchies. Both impose
substantial bandwidth penalties on sub-page tile patterns characteristic of NPU
workloads, and neither provides per-model key isolation or addresses UMA
shared-bus exposure.
Trusted Execution Environments. Several works [5,12] route DNN inference
through TEE-hosted code. Slalom [12] offloads linear layers to an untrusted GPU
while verifying results in an SGX enclave. These approaches, and PVM-style
secure-memory carve-outs, improve isolation but require gigabytes of weight data
to cross the TEE memory boundary or a statically reserved protected region,
incurring PCIe or shared-bus copy overhead and reducing usable main memory
for the rest of the system. Tessera eliminates this bottleneck by confining
decryption to an on-path ICE with zero data copies and no permanent DRAM
reservation.
Accelerator-specific confidential computing. NVIDIA H100 [11] Confiden-
tial Computing mode provides hardware attestation and memory encryption

for discrete data-center GPUs. This is architecturally closest to Tessera, but
targets PCIe-attached discrete GPUs under a hypervisor threat model rather
than UMA edge SoCs under OS compromise plus physical access. Our work
additionally addresses the nonce management and SRAM isolation challenges
specific to the shared physical bus.
On-chip SRAM protection. Hua et al. [14] protect model weights in TrustZone
secure SRAM, limiting supported model size to ≤4 MB. Tessera’s streaming
architecture removes this constraint by retaining only the active tile in plaintext
SRAM at any time, making the protected region independent of total model size.
DRAM bus security. Rowhammer [4,15] and Thunderclap [13] demonstrate
that DRAM physical attacks and IOMMU bypasses are practical on consumer
hardware. Tessera’s SMMU plus MTE tagging layer directly addresses these
attack classes.
8
Limitations and Future Work
Deployment considerations. Tessera is a reference architecture, and several
practical steps are required before it can be integrated into a shipping SoC.
Integrating Tessera into a production environment requires secure OEM provi-
sioning for the Device Identity Keypair and a verified boot chain to authenticate
the Secure World firmware that programs the SMMU stream-ID firewalls. Ad-
ditionally, OS scheduler cooperation is needed for the preemption hooks, and
power-management flows must safely handle ICE register state across low-power
transitions. While we expect these integration challenges to be standard for
dedicated hardware teams, we note them to clarify the boundary between our
reference architecture and silicon deployment.
Single-key-per-model. The current hierarchy provisions one kmsk per model
load. A natural extension is layer-granularity key rotation via HKDF sub-keys,
limiting the blast radius of a key compromise to a single layer.
Authenticated weight integrity. AES-CTR provides confidentiality but not
integrity, and this is a security-critical gap. An adversary with physical DRAM
write access (e.g., via a cold-boot-and-rewrite or bus interposer) can flip ciphertext
bits, causing controlled corruption of the decrypted weights without triggering
any alarm under the current design. For DNN workloads, even a small number of
bit flips can silently degrade model accuracy or, more dangerously, cause targeted
misclassification—a concern in safety-critical inference pipelines.
Mitigation sketch. The natural solution is authenticated encryption at tile
granularity. A promising approach is to pair each fetched cache line with a Message
Authentication Code (MAC) (e.g., AES-GMAC or Poly1305 [16]) computed
under a separate authentication key derived from kmsk via HKDF. To avoid the
prohibitive SRAM overhead of storing millions of tags on-chip, the architecture
would utilize an integrity tree (e.g., a Merkle tree) anchored by a single root
hash inside the Secure Enclave. A tag mismatch during tree traversal would halt
the DMA burst and raise a hardware fault, preventing corrupted weights from
reaching the NPU. Full design and timing analysis of this integrity extension are

left for future work; we consider it the highest-priority gap between this reference
architecture and production readiness.
Side-channel resistance. The threat model (section 2.4) excludes power-
analysis and EM side-channels, consistent with TrustZone assumptions. Nev-
ertheless, a co-located adversary process could potentially infer weight access
patterns through DRAM bus activity timing or shared-bus contention—a micro-
architectural leakage channel that does not require physical access. Mitigations
include: (i) instantiating the ICE’s AES-256 core with masked S-boxes and bal-
anced power consumption [17]; and (ii) traffic shaping on the ICE’s AXI port to
prevent bandwidth profiling from revealing layer-geometry information. A full
power-trace evaluation and traffic-shaping analysis are deferred to a future silicon
prototype.
9
Conclusion
We presented Tessera, a reference architecture for hardware-backed DRM on
UMA edge AI accelerators. By integrating an Inline Crypto Engine (ICE) into
the NPU’s AXI DMA fabric at cache-line granularity, Tessera eliminates the
structural bottlenecks of full-memory encryption. It provides hardware-enforced
isolation, preemption safety, and zero DRAM bandwidth amplification, all without
requiring statically reserved secure-memory carve-outs.
Our work grounds this design in concrete threat realities. We demonstrated a
live, exploit-free extraction of full model weights on a Jetson AGX Xavier via an
unmodified /dev/mem read, confirming UMA exposure as a critical vulnerability.
To counter this, our formal analysis proves that address-derived AES-CTR
counters are a strict cryptographic necessity to prevent plaintext leakage across
sparse DNN tensors.
Evaluations across three UMA platforms confirm that Tessera’s near-line-
rate pipelining safely hides keystream generation behind DRAM latency, even
under continuous DMA requests and timing variations (section 5.4). Projected to
a pipelined hardware model, Tessera sustains 22.1 GB/s—achieving 98.4% of
the raw DDR5-4800 bandwidth ceiling. In contrast, page-level encryption is struc-
turally capped at just 4.5 GB/s due to its massive 5× bandwidth amplification
factor.
Ultimately, Tessera emerges as a vital architectural primitive for edge AI.
By providing robust, near-zero-overhead DRM without permanently carving out
system memory, it delivers the hardware-backed security required for high-value
model IP on physically accessible, capacity-constrained edge SoCs.